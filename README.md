# decisio
A lightweight Java decision DSL that replaces nested if-else with fluent rule composition, supporting AND/OR/NOT predicate logic and functional execution flows. Note: DSL (Domain-Specific Language) is a specialized way of writing code that makes it feel like you are describing the problem, not programming it.
